import { StartSessionWizard } from "@/components/sessions/start-session-wizard";

export const metadata = {
  title: "Start session · RangeOps",
};

export default function NewSessionPage() {
  return <StartSessionWizard />;
}
